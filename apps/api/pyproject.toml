[project]
name = "rehearse-api"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "AsjalAbdullahButt", email = "asjal.abdullah.butt@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
rehearse-api = "rehearse_api:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
